services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: nexus-agent-backend:latest

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: nexus-agent-frontend:latest